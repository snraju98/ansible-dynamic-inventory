plugin: amazon.aws.aws_ec2

regions:
- us-east-1

hostnames:
 #- ip-address
 - private-ip-address

filters:
  tag:Name: frontend
  instance-state-name: running

keyed-groups:
 -key: tags.Name
   seperator: ''